export type TProduct = {
  id: string;
  name: string;
  image: string;
  description: string;
  weight: string;
  categoryId: string;
  createdBy?: string | null;
  createdAt: string;
  updatedAt: string;
};
